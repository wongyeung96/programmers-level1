# programmers
programmers algorism study
def solution(array, commands):
    answer = []
    for i in commands:
        sub = array[i[0]-1:i[1]]
        sub.sort()
        b = sub[i[2]-1]
        answer.append(b)
    return answer
